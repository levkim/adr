# CLAUDE.md — 프리라이드 파우더 다운힐 3D 게임 (실제 지형 기반)

## 프로젝트 개요

- 프리라이드 월드투어(FWT) 스타일의 3D 스노우보드/스키 게임
- **실존하는 설산을 실제 지형 데이터로 재현**하고, 정상에서 베이스까지 다운힐하는 한 판(런) 구조
- 구글어스로 답사하듯 장소를 고르고, 그 산을 게임 속에서 그대로 타는 것이 핵심 컨셉
- 사실성 최우선: 지형 형태, 경사, 능선/슈트/쿨와르 위치가 실제와 일치해야 함
- 라인 선택의 자유도: 클리프 드랍, 슈트, 내추럴 키커, 트리런을 플레이어가 직접 고른다
- 웹 브라우저 실행 (데스크톱 우선), 설치 없이 URL 공유로 플레이 가능 (정적 호스팅)

## 기술 스택

- Three.js + TypeScript + Vite
- 물리: heightmap 기반 자체 경량 물리 (지형 높이 샘플링 + 경사 벡터 슬라이딩). 전체 물리엔진(Rapier 등)은 꼭 필요해질 때까지 도입하지 않는다
- 외부 UI 프레임워크 없이 HTML/CSS 오버레이로 HUD 구성
- 디버그 튜닝: lil-gui
- 의존성 최소화. 새 라이브러리 추가 전에 반드시 이유를 설명할 것

## 실제 지형 재현 파이프라인 (핵심)

### 원칙

- 구글어스는 **장소 답사/라인 스카우팅용 시각 참고**로 사용한다 (좌표 확인, 경사/지형 파악)
- 구글어스의 3D 메시/이미지 데이터 자체는 추출·사용 금지 (이용약관 위반). 실제 지형 데이터는 공개 DEM(수치표고모델)에서 가져온다
- 게임 내 산은 "실제 좌표 범위의 실제 표고 데이터"로 만들어, 구글어스에서 본 산과 같은 형태여야 한다

### 데이터 소스

- 1차: AWS Terrain Tiles (Mapzen Terrarium, 전세계 무료 공개) — `https://s3.amazonaws.com/elevation-tiles-prod/terrarium/{z}/{x}/{y}.png`
- 보조: Copernicus GLO-30 DEM (30m 해상도), 일본 지역은 국토지리원(GSI) 고해상 DEM 검토
- 줌 레벨 12~14에서 타일을 받아 RGB→표고(m) 디코딩 후 heightmap으로 합성

### 구현 흐름

1. `locations.ts`에 장소 정의: 이름, 중심 위경도, 범위(bbox), 시작점(정상)·피니시(베이스) 좌표, 권장 줌
1. 빌드 타임 스크립트(`scripts/fetch-terrain.ts`)로 타일 다운로드 → 표고 디코딩 → 단일 heightmap(PNG 16bit 또는 바이너리)으로 베이크하여 `public/terrain/{location}/`에 저장 (런타임 외부 의존 제거)
1. 런타임에 heightmap 로드 → Three.js 지형 메시 생성 (수평/수직 스케일은 실제 비율 유지, 과장 금지)
1. 표고·경사·향(aspect) 기반 절차적 텍스처링: 설면, 암벽 노출부(급경사), 빙하 크레바스 톤, 수목한계선 아래 트리런
1. 나무/바위는 경사·고도 규칙으로 절차 배치 (위성사진 텍스처를 직접 붙이지 않는다 — 라이선스와 화질 문제)

### 초기 수록 장소 (후보)

- 벡 데 로스(Bec des Rosses), 스위스 베르비에 — FWT 결승 무대
- 하쿠바 핫포(Happo) 북사면, 일본 — 일본 파우더
- 알라아르차 인근 봉우리, 키르기스스탄 톈샨 — 자사 투어 지역 연계
- 장소는 1개로 시작해 파이프라인 검증 후 확장. 시드 대신 **장소 선택**이 시작 메뉴가 된다

## 코어 게임플레이

### 라이더 컨트롤

- 좌우: 카빙 턴 (속도에 따라 회전 반경 변화)
- 크라우치(웅크리기): 가속, 일어서기: 감속/제어
- 점프: 지형 경사 + 속도 기반 체공
- 공중: 그랩, 360/720 회전 트릭 (입력 조합)
- 착지: 보드 방향과 사면 방향 정렬도에 따라 클린 착지 / 휘청 / 낙상 판정

### 파우더 스노우 물리

- 눈 깊이 개념: 깊은 파우더는 감속 + 부유감, 다져진 눈은 빠름
- 경사각에 따른 가감속, 횡카빙 시 에지 그립 감쇠
- 향(북사면/남사면)에 따라 눈질 차이를 둘 수 있게 파라미터화

### 점수 (FWT 채점 모방)

- 라인 난이도: 통과 구간의 실제 경사/노출도/클리프 높이 가중치
- 에어 & 스타일: 트릭 종류, 회전수, 체공 시간
- 유려함(Fluidity): 정지/역주행/과도한 감속 시 감점
- 컨트롤: 낙상 시 큰 감점, 휘청 시 소감점
- 런 종료 시 저지 점수 카드 형태로 결과 표시 (100점 만점)

### 위험 요소

- 바위, 나무 충돌 → 낙상
- (확장) 급경사 고속 통과 시 눈사태 트리거 → 추격 연출

## 카메라 시스템 (다중 시점 — 핵심 요구사항)

`C` 키(또는 UI 버튼)로 순환 전환. 모든 모드는 전환 시 부드럽게 보간.

1. **3인칭 추적 (기본)**: 라이더 후방, 속도에 따라 거리/FOV 동적 변화. 점프 시 풀백, 착지 셰이크
1. **1인칭 고글 뷰**: 헬멧캠 시점. 고글 프레임 비네팅 오버레이, 시야 하단에 보드 팁/장갑 일부 노출, 속도·턴 연동 헤드 모션(과하지 않게), 멀미 방지용 모션 강도 설정 제공
1. **숄더 캠**: 어깨 너머 근접 시점 — 파우더 스프레이가 화면을 가장 많이 덮는 몰입 모드
1. **드론/시네마틱**: 측면·전방 고정 추적, 리플레이용
1. (확장) 런 종료 후 자동 리플레이: 시네마틱 캠으로 하이라이트 재생

## 파우더 비주얼 — 얼굴/몸/시야에 흩날리는 눈 (핵심 요구사항)

### 월드 파티클

- 턴/착지 시 라이더 주변 파우더 스프레이 (속도·턴 강도 비례, GPU 파티클)
- 스프레이가 라이더 몸을 통과하며 흩날리는 연출: 라이더 메시 주변 보조 이미터
- 깊은 파우더 고속 주행 시 가슴 높이 "화이트룸" 연출 (시야 일부가 눈으로 덮임)

### 카메라/시야 부착 효과 (1인칭·숄더 캠에서 특히 중요)

- 카메라에 부착된 근접 파티클 이미터: 눈입자가 시야를 향해 날아와 스쳐 지나감
- **고글 렌즈 효과**: 스프레이를 맞으면 화면에 눈 입자가 튀어 붙고(스크린 스페이스 스플래터) 수 초에 걸쳐 녹아 흘러내리며 사라짐. 김서림/성에 미세 효과
- 바람·강설 환경 파티클: 카메라 전방 원뿔 영역에만 렌더링해 비용 절감
- 모든 화면 효과는 강도 0~100% 설정 가능 (가독성/멀미 배려)

### 라이더 몸 표현

- 3인칭에서 의류·헬멧에 눈 묻음(셰이더 기반 누적, 시간/충격으로 감소)
- 낙상 시 전신 파우더 폭발 + 눈 묻음 최대치

## 환경 & 사실성

- 시간대/광원 프리셋: 블루버드(쾌청), 햇빛 낮은 아침, 강설
- 대기 원근(거리 안개), 설면 스파클, 태양 방향 기준 사면 음영 — 실제 산 사진과 비교하며 톤 맞추기
- 사운드: 에지 카빙음, 파우더 슬러시음, 바람(속도 연동), 착지 임팩트, 1인칭에서는 호흡음 추가

## HUD / UI

- 인게임: 속도, 고도(남은 표고), 점수/콤보, 트릭 팝업, 현재 카메라 모드 표시
- 시작 화면: **장소 선택(실제 산 카드 + 지도/표고 정보)**, 드랍 인 지점 선택, 출발
- 결과 화면: 항목별 저지 점수, 라인 궤적을 산 위에 표시, 다시 하기
- 텍스트는 한국어 기본, 영어 토글 가능 구조

## 성능 목표

- 데스크톱 60fps (중급 노트북 기준)
- 지형 청크 + LOD (실측 DEM은 폴리곤이 많으므로 필수), 나무/바위 InstancedMesh
- 파티클 GPU화 + 풀링, 루프 내 객체 생성 금지
- 카메라 모드별 파티클 예산 분리 (1인칭은 근접 파티클에 예산 집중)

## 파일 구조

```
src/
  main.ts            # 엔트리, 게임 루프
  config.ts          # 모든 튜닝 파라미터 (매직넘버 금지)
  core/              # loop, input, state
  world/
    locations.ts     # 실제 장소 정의 (좌표, bbox, 스타트/피니시)
    terrain.ts       # heightmap 로드/메시 생성/높이 샘플링
    surface.ts       # 경사·고도 기반 텍스처/눈질
    props.ts         # 나무/바위 절차 배치
  player/            # controller, physics, tricks, 눈 묻음 셰이더
  camera/            # 모드별 카메라 + 전환 보간
  fx/                # 파우더 파티클(월드/카메라 부착), 고글 스플래터, audio
  scoring/
  ui/
scripts/
  fetch-terrain.ts   # DEM 타일 다운로드 → heightmap 베이크
public/terrain/      # 베이크된 장소별 heightmap
```

## 코딩 규칙

- TypeScript strict 모드
- 물리/연출 상수는 전부 config.ts + lil-gui 런타임 튜닝
- 한 작업당 커밋 하나, 커밋 메시지는 한국어로 간결하게
- 요청하지 않은 리팩터링 금지
- 각 단계 완료 후 `npm run dev`로 직접 플레이하여 조작감·사실감을 확인하고 보고할 것
- 지형 사실성 검증: 생성된 지형 스크린샷을 구글어스 같은 앵글과 비교해 형태 일치 확인

## 개발 단계 (이 순서대로 진행)

1. **셋업**: Vite + Three.js + TS, 평평한 지면 + 캡슐 라이더 이동
1. **실제 지형 파이프라인**: fetch-terrain 스크립트 → 1개 장소(벡 데 로스) heightmap 베이크 → 지형 메시 렌더 → 구글어스와 형태 비교 검증
1. **활강 물리**: 실측 지형 위 중력/경사 슬라이딩 + 높이 샘플링 충돌
1. **조작감**: 카빙/크라우치/점프 + 3인칭 추적 카메라 — 여기서 조작감을 충분히 튜닝
1. **카메라 시스템**: 1인칭 고글 뷰/숄더/드론 모드 + 전환 보간
1. **파우더 연출**: 월드 스프레이 → 카메라 부착 파티클 → 고글 스플래터/화이트룸 → 사운드
1. **콘텐츠**: 절차 배치(나무/바위), 충돌·낙상, 위험 요소
1. **점수**: 트릭 인식 + FWT식 채점 + 결과 화면(라인 궤적)
1. **마감**: 최적화, 장소 추가(하쿠바·키르기스스탄), 시작 화면, (선택) 모바일

## 실행 / 테스트

- 지형 베이크: `npm run terrain -- --location=bec-des-rosses`
- 개발: `npm run dev` / 타입 체크: `npm run typecheck` (커밋 전 필수) / 빌드: `npm run build`
- 자동 테스트보다 수동 플레이 검증 우선. 물리·파티클 파라미터 변경 시 반드시 직접 런을 타보고 판단

## 하지 말 것

- 구글어스/구글맵스의 3D 데이터·위성 이미지를 직접 추출하거나 텍스처로 사용하지 않기
- 지형 표고를 임의로 과장하지 않기 (사실성 원칙)
- 조작감(4단계) 확인 전에 연출/콘텐츠로 넘어가지 않기
- 무거운 물리엔진·게임 프레임워크 도입을 먼저 논의 없이 제안하지 않기
- 한 번에 여러 단계를 몰아서 구현하지 않기
